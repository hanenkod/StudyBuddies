doctype html
html
  head
    meta(charset="utf-8")
    link(rel="stylesheet", href="/css/globals.css")
    block styles
    title StudyBuddies
  body
    block navbar
      .navbar
        a(href="/").nav-link Home
        a(href="/tutors").nav-link Tutors
        if currentUser
          a(href=(currentUser.type === 'tutor' ? `/tutor-profile/${currentUser.id}` : `/user-profile/${currentUser.id}`)).nav-link Profile
        else
          a(href="/login").nav-link Login
        a(href="/chats").nav-link Chats
    block content

    block footer
      footer.footer
        p © 2025 Study Buddies

    block scripts